# BPMN TO-BE — Целевой процесс модерации с ML-системой

> Бизнес-процесс **после** внедрения ML-системы анализа тональности.  
> Акцент: 100 % охват, автоматическое решение по явным случаям, ручная проверка только пограничных (~10–15 %).

```mermaid
flowchart TD
    A([Пользователь публикует комментарий]) --> B[Comment Ingestion Service\nсохраняет в БД + отправляет в Kafka\nтопик: comments.raw]
    
    B --> C[Sentiment Inference Service\nанализирует тональность\n⏱ ≤ 50 мс]
    
    C --> D[Результат записывается в\ncomments.scored\nlabel + scores + model_version]
    
    D --> E{Оценка\nтоксичности?}
    
    E -->|score_toxic ≥ 0.92\nАвтоблок| F[Moderation Service:\nавтоматически скрывает комментарий\nстатус: auto_blocked]
    
    E -->|0.70 ≤ score_toxic < 0.92\nПограничный случай| G[Moderation Service:\nставит в очередь ручной проверки\nстатус: pending_review]
    
    E -->|score_toxic < 0.70\nБезопасный| H[Комментарий остаётся опубликованным\nстатус: approved_auto]
    
    F --> F1[Уведомление в Admin Dashboard\nзапись в audit_log]
    F1 --> F2{Апелляция\nпользователя?}
    F2 -->|Да| G
    F2 -->|Нет| Z1([Конец: скрыт])
    
    G --> I[Модератор проверяет вручную\n~10–15 % потока]
    I --> J{Решение\nмодератора}
    J -->|Подтвердить блок| K[Скрыть / удалить\nобновить статус]
    J -->|Опубликовать| L[Опубликовать\nобновить статус: published]
    J -->|Передать старшему| M[Эскалация\nстаршему модератору]
    M --> J
    
    K --> Z2([Конец: удалён])
    L --> Z3([Конец: опубликован])
    H --> Z4([Конец: опубликован автоматически])

    style A fill:#d4edda,stroke:#28a745
    style C fill:#fff3cd,stroke:#ffc107
    style F fill:#f8d7da,stroke:#dc3545
    style H fill:#d4edda,stroke:#28a745
    style Z1 fill:#cce5ff,stroke:#004085
    style Z2 fill:#cce5ff,stroke:#004085
    style Z3 fill:#cce5ff,stroke:#004085
    style Z4 fill:#cce5ff,stroke:#004085
```

## Что изменяет ML-система (сравнение AS-IS vs. TO-BE)

| Параметр | AS-IS (без ML) | TO-BE (с ML) | Изменение |
|---|---|---|---|
| Охват проверки | ~2 % | 100 % | **×50** |
| Время реакции | Часы | ≤ 30 сек | **×360** |
| Нагрузка на модераторов | 100 % потока (выборочно) | 10–15 % потока (пограничные) | **−85 %** |
| Консистентность решений | Субъективная | Детерминированная (порог) | **+** |
| Аналитика тональности | Нет | Дашборд в реальном времени | **Новая возможность** |
| Риск штрафов РКН | Высокий | Низкий (recall ≥ 0.95) | **−** |
| Операционные расходы | ≥ 20 модераторов | 5–7 модераторов | **−65 % ФОТ** |
